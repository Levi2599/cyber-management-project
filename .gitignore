# Lesson videos and heavy media files
*.mp4
*.avi
*.mkv
*.mov

# n8n backups containing credentials (crucial to prevent GitHub Push Protection blocking)
workflow-backup-*.json
workflow-*-backup.json
n8n-backups/

# n8n database, logs and keys
.n8n/
*.sqlite
*.log
.env
.agents/

# Evaluation output — regenerated by scripts/eval/run_eval.py, not source
scripts/eval/results.json

# ---------------------------------------------------------------------------
# Course material and local working files.
# This directory doubles as the course folder on disk; these files stay locally
# but are deliberately kept out of the repository, which is a project showcase.
# ---------------------------------------------------------------------------
שייעור*/
שיעור*
תוכנית עבודה טכנית*
פרויקט גמר קורס*
פרוייקט סייבר*
סיכומים-לימוד עצמי/
.tmp-*/

# Submission bundle — duplicates files that already live at the repo root
ready_for_upload/

# Generated / superseded artefacts
PRD*.docx
dual_code_auditor_wf.json
site/

# One-off scratch scripts used while building the project
backup_workflow.py
copy_humanizer.py
extract_to_txt.py
inspect_backup.py
list_all_backup_names.py
read_attached_report.py
search_backup.py

# Python caching and environments
__pycache__/
*.pyc
*.pyo
venv/
.venv/
env/
.env/

# IDE and OS files
.vscode/
.idea/
.claude/
Thumbs.db
Desktop.ini
.DS_Store

# Superseded by scripts/generate_report.py — the old version contains
# hard-coded benchmark numbers that were never measured. Kept on disk only.
/generate_report.py
